module Mcq {
    export module Home {
        export function resetPasswordController($scope: IPaymentScope, $stateParams, $rootScope, $state, accountService: AccountService, growl) {
           
        }
    }
}

Mcq.Home.resetPasswordController.$inject = ["$scope", "$stateParams", "$rootScope", "$state", "accountService", "growl"];    